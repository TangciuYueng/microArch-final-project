<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="cn.edu.tongji.login.mapper.FriendMapper">
    <select id="getFriendByUserId" resultType="friendInfo">
        SELECT friend_id AS id, name, avatar, intimacy
        FROM friend AS f, user As u
        WHERE f.friend_id = u.id AND f.user_id = #{userId};
    </select>

    <insert id="addFriend" parameterType="addFriendRequest">
        INSERT INTO friend(user_id, friend_id)
        VALUES (#{userId}, #{friendId});
    </insert>

    <update id="updateFriendIntimacy" parameterType="updateFriendIntimacyRequest">
        UPDATE friend
        SET intimacy = intimacy + #{variation}
        WHERE user_id = #{userId} AND friend_id = #{friendId};
    </update>

    <delete id="deleteFriend" parameterType="deleteFriendRequest">
        DELETE FROM friend
        WHERE user_id = #{userId} AND friend_id = #{friendId};
    </delete>
</mapper>